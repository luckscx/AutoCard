# 前端分析

本文档合并自：FRONTEND_ANALYSIS.md、AUTOCARD_EXPLORATION_REPORT.md、CODE_EXPLORATION_SUMMARY.md、READ_ME_FIRST_EXPLORATION.md、QUICK_REFERENCE_LEVEL_BOARD.md

---

# AutoCard 前端代码结构分析

## 目录结构概览

```
client/src/
├── main.ts                    # 应用入口（初始化 Pixi.js 应用）
├── api/
│   └── client.ts             # API 客户端（后端通信）
├── core/
│   ├── GameState.ts          # 游戏状态全局管理
│   └── SceneManager.ts       # 场景管理器
├── scenes/                   # 四个主要场景
│   ├── LobbyScene.ts         # 大厅/登录场景
│   ├── MainScene.ts          # 主运营场景（日常运营/三选一）
│   ├── BattleScene.ts        # 战斗回放场景
│   └── ShopScene.ts          # 商店/集市场景
└── ui/                       # UI 组件
    ├── Button.ts             # 按钮组件
    ├── CardView.ts           # 卡牌显示组件（商店/卡牌库）
    ├── BattleCardView.ts     # 战斗中卡牌组件（带动画）
    ├── BoardRow.ts           # 棋盘行（拖拽排列）
    ├── BottomBar.ts          # 底部信息栏（HP/金币/储物箱）
    ├── SlotGrid.ts           # 槽位网格
    ├── Panel.ts              # 面板基类
    ├── targetSlotPreview.ts  # 目标槽位预览
    └── layout.ts             # 布局常量和辅助函数
```

## Scene 文件列表

| 文件名 | 用途 | 关键功能 |
|--------|------|---------|
| **LobbyScene.ts** | 大厅/连接 | 加载配置、英雄选择、账户管理 |
| **MainScene.ts** | 主场景 | 日常运营、PvE/PvP选择、三选一 |
| **BattleScene.ts** | 战斗回放 | 播放战斗动画、显示伤害/效果、结果界面 |
| **ShopScene.ts** | 商店 | 购买卡牌、卖出卡牌、刷新商店 |

**关键路径**：
```
LobbyScene → MainScene → (Battle/Shop) → MainScene → ...
```

## 战斗回放相关文件

### 主要文件
- **BattleScene.ts** (552 行)
  - 事件驱动的战斗回放系统
  - 支持 PvE 和 PvP 战斗
  - 实时动画播放（4x 速度）

### 战斗事件类型
```typescript
- card_trigger: 卡牌触发（显示 flash 动画）
- damage: 伤害飘字 (-${value}, #ff4444 红色)
- heal: 治疗飘字 (+${value}, #44ff44 绿色)
- shield: 护盾飘字 (🛡️+${value}, #88ccff 蓝色)
- poison: 中毒飘字 (☠️+${value}, #88ff44 绿色)
- burn: 燃烧飘字 (🔥+${value}, #ff8844 橙色)
- dot_tick: 持续伤害 (-${total} DoT, #cc66ff 紫色)
- destroy: 摧毁 (💥摧毁, #ff2222 红色)
- overtime: 加时伤害
- haste/slow/freeze/charge: 状态效果飘字
- battle_end: 战斗结束
```

## UI 交互相关文件

### 按钮交互
**Button.ts** (35 行)
```typescript
- 悬停变色（0xcccccc tint）
- pointertap 事件处理
- 禁用状态支持
```

### 棋盘拖拽操作
**BoardRow.ts** (280+ 行)
- onSwap：交换卡牌
- onMerge：合并卡牌
- onDragOut：拖出操作
- onDragging：拖拽中
- onDragStop：拖拽停止

### 布局和定位
**layout.ts** (74 行)
```typescript
- 4 个 Zone 区域定义（Z1-Z4）
- 卡牌尺寸常量
- Tier 颜色系统
- 响应式 cardWidth() 函数
```

## UI 层级结构

```
Pixi.js Application
└── Stage
    ├── Z1 背景 (Y: 2, H: 100)
    │   ├── 标题文本
    │   ├── 按钮
    │   └── 刷新状态
    ├── Z2 背景 (Y: 106, H: 230)
    │   ├── 商品卡牌
    │   ├── 选择按钮
    │   ├── 储物箱
    │   └── 事件面板
    ├── Z3 背景 (Y: 340, H: 156)
    │   ├── 棋盘标签
    │   └── BoardRow (卡牌拖拽)
    ├── Z4 底栏 (Y: 500, H: 100)
    │   ├── HP 显示
    │   ├── 金币显示
    │   └── 储物箱按钮
    ├── Floating Layer (飘字)
    ├── Sell Hint (拖拽售出提示)
    └── Stash Panel (储物箱面板)
```

## API 交互点

**client.ts** 提供的关键 API：
```typescript
- getHeroes()：获取英雄列表
- getItems()：获取物品列表
- getBazaarItems()：获取集市物品
- getUserMe()：获取用户信息
- getCurrentRun()：获取当前运营
- buy(runId, itemId, target, slotIndex)
- sellItem(runId, container, slotIndex)
- swapItems(runId, container, idx1, idx2)
- mergeItems(runId, container, indexA, indexB)
- placeItem(runId, fromContainer, fromSlot, toContainer, toSlot)
- refreshShop(runId)
- leaveShop(runId)
```

## 关键文件统计

| 类别 | 文件数 | 总行数 | 说明 |
|------|--------|--------|------|
| Scene 场景 | 4 | ~1800 | 核心逻辑 |
| UI 组件 | 9 | ~1200 | 界面组件 |
| Core | 3 | ~900 | 管理器 |
| API | 1 | ~200 | 后端通信 |
| **总计** | **17** | **~4100** | 纯前端代码 |

---

# AutoCard 项目代码探索报告

## 等级 (Level) 相关定义

### shared/src/constants.ts
```typescript
export const XP_PER_LEVEL = 8;  // 升级所需的 XP

// 每个等级对应的棋盘可用格数（最多 10 格）
// level 1→4格, 2→6格, 3→8格, 4+→10格 (每级增加 2 格)
export const BOARD_SLOTS_BY_LEVEL: Record<number, number> = {
  1: 4, 2: 6, 3: 8,
};

export function boardSlotsForLevel(level: number): number {
  return BOARD_SLOTS_BY_LEVEL[level] ?? 10;
}

// 商店刷新金币随等级提高
export function shopRefreshCostForLevel(level: number): number {
  if (level < 3) return SHOP_REFRESH_COST.bronze;    // 2金币
  if (level < 5) return SHOP_REFRESH_COST.silver;    // 4金币
  if (level < 8) return SHOP_REFRESH_COST.gold;      // 6金币
  return SHOP_REFRESH_COST.diamond;                  // 8金币
}
```

### shared/src/types/game.ts - RunState 接口
```typescript
export interface RunState {
  id: string;
  userId: string;
  heroId: string;
  level: number;              // ⭐ 玩家当前等级
  xp: number;
  boardSlots: number;         // ⭐ 当前可用棋盘格数（4~10）
  board: SlotItem[];          // 玩家棋盘 (4~10 格)
  stash: SlotItem[];          // 储物箱 (10 格)
  pendingLevelUp?: PendingLevelUpState | null;  // 升级三选一状态
  // ... 其他字段
}
```

## 棋盘 (Board) 和格子 (Slot) 相关定义

### shared/src/types/game.ts - SlotItem 接口
```typescript
export interface SlotItem {
  itemId: string;
  tier: Tier;                      // bronze|silver|gold|diamond|legendary
  size: ItemSize;                  // 1 | 2 | 3（占用的格数）
  slotIndex: number;               // 在棋盘/储物箱中的起始位置
}
```

## 服务端 - 玩家等级处理

### server/src/services/RunService.ts - 升级逻辑

```typescript
private gainXp(run: IRun, amount: number) {
  run.xp += amount;
  
  while (run.xp >= XP_PER_LEVEL) {
    run.xp -= XP_PER_LEVEL;
    run.level += 1;
    
    const hero = HEROES.find(h => h.heroId === run.heroId);
    if (hero) {
      run.maxHp += hero.hpPerLevel;
      run.hp = Math.min(run.maxHp, run.hp + hero.hpPerLevel);
    }
    
    run.boardSlots = boardSlotsForLevel(run.level);
    
    if (!run.pendingLevelUp) {
      run.pendingLevelUp = this.generateLevelUpChoices(run);
      run.markModified('pendingLevelUp');
    }
  }
}
```

#### 升级三选一选项生成
```typescript
private generateLevelUpChoices(run: IRun) {
  const canUnlockSlot = run.boardSlots < 10;
  return {
    level: run.level,
    choices: [
      canUnlockSlot
        ? { label: '解锁棋盘格（+1格）', kind: 'unlock_slot' }
        : { label: '生命上限 +5', kind: 'bonus_hp' },
      { label: '升阶一张卡牌（随机）', kind: 'upgrade_item' },
      { label: '生命上限 +10，治疗 10', kind: 'bonus_hp_heal' },
    ],
  };
}
```

#### 商店物品生成（与等级挂钩）
```typescript
private tierPickWeight(level: number, tier: string): number {
  if (level < 3) return tier === 'bronze' ? 1 : 0;      // 只有 bronze
  if (level < 5) {
    return tier === 'bronze' ? 1 : tier === 'silver' ? 0.5 : 0;
  }
  if (level < 8) {
    return tier === 'legendary' ? 0 : tier === 'diamond' ? 0.3 : 1;
  }
  return tier === 'legendary' ? 0.2 : 1;
}
```

## 客户端 - 棋盘渲染

### client/src/ui/layout.ts - 棋盘尺寸常量
```typescript
export const CARD_UNIT = 88;      // 单个卡牌单位宽度
export const CARD_GAP = 6;        // 卡牌间距
export const CARD_H = 110;        // 卡牌高度

export function cardWidth(size: 1 | 2 | 3): number {
  return CARD_UNIT * size + CARD_GAP * (size - 1);
  // size=1: 88   size=2: 182   size=3: 276
}
```

### client/src/scenes/MainScene.ts - 主场景
```typescript
async onEnter() {
  const run = gameState.run!;
  const boardSlots = run.boardSlots ?? 10;
  this.boardRow = new BoardRow(boardSlots);
  this.boardRow.update(run.board);

  // 储物箱始终 10 格
  this.stashRow = new BoardRow(10);
  this.stashRow.update(run.stash);
}

// 当升级时动态调整棋盘格数
private refresh() {
  const run = gameState.run!;
  const newBoardSlots = run.boardSlots ?? 10;
  
  if (this.boardRow.slotCount !== newBoardSlots) {
    this.removeChild(this.boardRow);
    this.boardRow = new BoardRow(newBoardSlots);  // ⭐ 重新创建
    this.addChild(this.boardRow);
  }
  
  this.boardRow.update(run.board);
}
```

## 关键数据流总结

### 升级 → 解锁格子流程
```
1. PvE 或 PvP 战斗获胜
   ↓
2. 获得 XP，调用 gainXp()
   ↓
3. 当 xp >= 8 时，level++，boardSlots 更新
   ↓
4. 生成 pendingLevelUp 三选一
   ↓
5. 玩家选择 → handleLevelUpChoice()
   ↓
6. pendingLevelUp 清空，返回主场景
   ↓
7. MainScene.refresh() 检测 boardSlots 变化
   ↓
8. 重建 BoardRow，格子数 +1 ✓
```

### 等级→商店物品类型映射
```
Level 1-2   → 仅 Bronze 品质
Level 3-4   → Bronze 为主，Silver 偶现
Level 5-7   → 全系出现，Legendary 不出
Level 8+    → 全系出现，包括 Legendary
```

## 关键文件路径速查表

| 功能 | 文件路径 | 关键对象 |
|------|---------|--------|
| **等级常量** | `shared/src/constants.ts` | `XP_PER_LEVEL`, `boardSlotsForLevel()` |
| **类型定义** | `shared/src/types/game.ts` | `RunState`, `SlotItem` |
| **Run 模型** | `server/src/models/Run.ts` | `IRun` interface |
| **升级逻辑** | `server/src/services/RunService.ts` | `gainXp()`, `generateLevelUpChoices()` |
| **棋盘尺寸** | `client/src/ui/layout.ts` | `CARD_UNIT`, `cardWidth()` |
| **棋盘组件** | `client/src/ui/BoardRow.ts` | `BoardRow` class |
| **卡牌组件** | `client/src/ui/CardView.ts` | `CardView` class |
| **信息栏** | `client/src/ui/BottomBar.ts` | 显示 level, xp, pvpWins |
| **主场景** | `client/src/scenes/MainScene.ts` | 棋盘初始化和动态调整 |

---

# 等级、棋盘、格子 快速参考

## 核心数据结构

### RunState (玩家运行状态)
```
level: number                  // 当前等级 (1 起始)
boardSlots: number            // 可用棋盘格数 (4~10)
xp: number                    // 经验值 (0~8)
board: SlotItem[]             // 棋盘上的卡牌数组
stash: SlotItem[]             // 储物箱的卡牌数组 (固定 10 格)
pendingLevelUp: {             // 升级三选一状态
  level: number
  choices: { label, kind }[]
}
```

### SlotItem (棋盘卡牌)
```
itemId: string                // 物品 ID
tier: Tier                    // 等阶 (bronze|silver|gold|diamond|legendary)
size: 1|2|3                   // 占用格数
slotIndex: number             // 在棋盘中的起始位置 (0-indexed)
```

## 关键常数

| 常数 | 值 | 说明 |
|------|-----|------|
| `XP_PER_LEVEL` | 8 | 升级所需 XP |
| `BOARD_SIZE` | 10 | 最大棋盘格数 |
| `STASH_SIZE` | 10 | 储物箱固定格数 |

## 等级 → 棋盘格数 映射

| 等级 | 棋盘格数 |
|------|----------|
| 1 | 4 |
| 2 | 6 |
| 3 | 8 |
| 4+ | 10 |

**代码位置**：`shared/src/constants.ts` → `BOARD_SLOTS_BY_LEVEL`

## 关键代码位置

| 需求 | 文件 | 行号 |
|------|------|-----|
| 升级条件 | `shared/src/constants.ts` | L4 |
| 格子映射 | `shared/src/constants.ts` | L59-64 |
| RunState 定义 | `shared/src/types/game.ts` | L54-77 |
| SlotItem 定义 | `shared/src/types/game.ts` | L27-32 |
| Run 模型 | `server/src/models/Run.ts` | L49-78 |
| gainXp 逻辑 | `server/src/services/RunService.ts` | L505-523 |
| 升级选项生成 | `server/src/services/RunService.ts` | L525-537 |
| 棋盘尺寸常数 | `client/src/ui/layout.ts` | L4-10 |
| BoardRow 组件 | `client/src/ui/BoardRow.ts` | 全文 |
| 主场景 | `client/src/scenes/MainScene.ts` | L85-96, 314-329 |

## 快速检查清单

- [ ] 玩家等级范围：1 ~ ∞
- [ ] 棋盘格数范围：4 ~ 10
- [ ] 升级需要 XP：8
- [ ] 默认棋盘格数（等级 1）：4
- [ ] 最大棋盘格数：10（等级 4+）
- [ ] 储物箱格数：固定 10
- [ ] 卡牌尺寸选项：1, 2, 3
- [ ] 棋盘动态重建：YES（detectSlotCount 变化时）

## 常见修改点

### 修改升级 XP 要求
📁 文件：`shared/src/constants.ts:4`  
🔧 改动：`export const XP_PER_LEVEL = 8;` → 改为其他值

### 修改等级→格数映射
📁 文件：`shared/src/constants.ts:59-64`  
🔧 改动：编辑 `BOARD_SLOTS_BY_LEVEL` 对象

### 修改卡牌渲染大小
📁 文件：`client/src/ui/layout.ts:4-10`  
🔧 改动：改变 `CARD_UNIT` 或 `CARD_GAP` 值

---

# AutoCard 项目代码探索 — 文件清单

## 已探索关键文件

### Shared (共享层)

| 文件 | 关键内容 | 核心对象 |
|------|---------|--------|
| `shared/src/constants.ts` | **等级系统常数** | `XP_PER_LEVEL`=8, `BOARD_SLOTS_BY_LEVEL`, `boardSlotsForLevel()` |
| `shared/src/types/game.ts` | **游戏状态类型** | `RunState`, `SlotItem`, `PendingLevelUpState` |
| `shared/src/types/config.ts` | **配置类型** | `ItemConfig` (包含 size 字段) |

### Server (后端服务)

| 文件 | 关键内容 | 核心函数 |
|------|---------|--------|
| `server/src/models/Run.ts` | **MongoDB 数据模型** | `IRun` interface |
| `server/src/services/RunService.ts` | **游戏逻辑核心** | `startRun()`, `gainXp()`, `generateLevelUpChoices()` |

### Client (前端渲染)

| 文件 | 关键内容 | 核心类 |
|------|---------|--------|
| `client/src/ui/layout.ts` | **棋盘尺寸常数** | `CARD_UNIT`, `cardWidth()` |
| `client/src/ui/BoardRow.ts` | **棋盘行组件** | `BoardRow` class, `slotCount` |
| `client/src/ui/CardView.ts` | **卡牌视图** | `CardView` class |
| `client/src/ui/BottomBar.ts` | **玩家信息栏** | 显示 level, xp, pvpWins |
| `client/src/scenes/MainScene.ts` | **主游戏场景** | 棋盘初始化和动态调整 |
| `client/src/scenes/ShopScene.ts` | **商店场景** | 商品展示和购买 |
| `client/src/scenes/BattleScene.ts` | **战斗场景** | 战斗结果处理 |
| `client/src/core/GameState.ts` | **游戏全局状态** | 维护当前 run 状态 |

## 关键数值汇总

| 项目 | 值 | 来源 |
|------|-----|------|
| 升级所需 XP | 8 | `XP_PER_LEVEL` |
| 初始棋盘格数 | 4 | `BOARD_SLOTS_BY_LEVEL[1]` |
| 最大棋盘格数 | 10 | `boardSlotsForLevel(7+)` |
| 储物箱固定格数 | 10 | `new BoardRow(10)` |
| 卡牌单位宽 | 88 | `CARD_UNIT` |
| 卡牌高度 | 110 | `CARD_H` |
| 卡牌间距 | 6 | `CARD_GAP` |
| 卡牌最大尺寸 | 3 格 | `ItemSize = 1\|2\|3` |
